apiVersion: v1
kind: ConfigMap
metadata:
  name: grafana-datasources
  namespace: vibraguard
data:
  prometheus.yaml: |
    apiVersion: 1
    datasources:
      - name: prometheus
        type: prometheus
        access: proxy
        url: http://prometheus-server:80
        isDefault: true
