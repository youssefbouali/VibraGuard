apiVersion: apps/v1
kind: Deployment
metadata:
  name: workorder-service
  namespace: vibraguard
spec:
  replicas: 1
  selector:
    matchLabels:
      app: workorder-service
  template:
    metadata:
      labels:
        app: workorder-service
    spec:
      containers:
        - name: workorder-service
          image: vibraguard-workorder-service:latest
          imagePullPolicy: Never
          ports:
            - containerPort: 8084
          env:
            - name: DB_URL
              value: "jdbc:oracle:thin:@oracle-db.vibraguard.svc.cluster.local:1521:xe"
            - name: DB_USERNAME
              valueFrom:
                secretKeyRef:
                  name: oracle-db-credentials
                  key: username
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: oracle-db-credentials
                  key: password
---
apiVersion: v1
kind: Service
metadata:
  name: workorder-service
  namespace: vibraguard
spec:
  selector:
    app: workorder-service
  ports:
    - port: 8084
      targetPort: 8084
