apiVersion: apps/v1
kind: Deployment
metadata:
  name: motor-service
  namespace: vibraguard
spec:
  replicas: 1
  selector:
    matchLabels:
      app: motor-service
  template:
    metadata:
      labels:
        app: motor-service
    spec:
      containers:
        - name: motor-service
          image: vibraguard-motor-service:latest
          imagePullPolicy: Never
          ports:
            - containerPort: 8082
          env:
            - name: DB_URL
              value: "jdbc:oracle:thin:@oracle-db.vibraguard.svc.cluster.local:1521:xe"
            - name: DB_USERNAME
              valueFrom:
                secretKeyRef:
                  name: oracle-db-credentials
                  key: username
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: oracle-db-credentials
                  key: password
---
apiVersion: v1
kind: Service
metadata:
  name: motor-service
  namespace: vibraguard
spec:
  selector:
    app: motor-service
  ports:
    - port: 8082
      targetPort: 8082
